<Window x:Class="WPF.BaoCaoChamCong"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Báo Cáo Chấm Công" Height="500" Width="850" Background="#f2f2f2">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="Báo Cáo Chấm Công Hàng Tháng"
                   FontSize="24" FontWeight="Bold" Foreground="#333"
                   HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <!-- Nhập tháng và năm -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Tháng:" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <ComboBox Name="MonthComboBox" Width="80">
                <ComboBoxItem Content="1"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="3"/>
                <ComboBoxItem Content="4"/>
                <ComboBoxItem Content="5"/>
                <ComboBoxItem Content="6"/>
                <ComboBoxItem Content="7"/>
                <ComboBoxItem Content="8"/>
                <ComboBoxItem Content="9"/>
                <ComboBoxItem Content="10"/>
                <ComboBoxItem Content="11"/>
                <ComboBoxItem Content="12"/>
            </ComboBox>

            <TextBlock Text="Năm:" FontSize="16" Margin="20,0,10,0" VerticalAlignment="Center"/>
            <TextBox Name="YearTextBox" Width="80"/>

            <Button Content="Xác nhận" Width="100" Height="30" Margin="20,0,0,0"
                    Background="#007BFF" Foreground="White" FontWeight="Bold" Click="GenerateReport_Click"/>
        </StackPanel>

        <!-- Bảng hiển thị báo cáo -->
        <DataGrid Name="ReportDataGrid" Grid.Row="2" AutoGenerateColumns="False"
                  BorderThickness="1" BorderBrush="Gray" RowBackground="White"
                  AlternatingRowBackground="#E3E3E3">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Họ và tên" Binding="{Binding FullName}" Width="150"/>
                <DataGridTextColumn Header="Tổng ngày đi làm" Binding="{Binding TotalWorkDays}" Width="150"/>
                <DataGridTextColumn Header="Tổng ngày nghỉ có phép" Binding="{Binding TotalLeaveDays}" Width="150"/>
                <DataGridTextColumn Header="Tổng ngày nghỉ không phép" Binding="{Binding TotalUnexcusedLeaveDays}" Width="170"/>
                <DataGridTextColumn Header="Tổng giờ làm trong tháng" Binding="{Binding TotalWorkHours}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>