<Window x:Class="WPF.BaoCaoChamCong"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Báo Cáo Chấm Công" Height="500" Width="850" Background="#f2f2f2">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Text="Báo Cáo Chấm Công Hàng Tháng"
                   FontSize="24" FontWeight="Bold" Foreground="#333"
                   HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <!-- Nhập tháng và năm -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Tháng:" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <ComboBox Name="MonthComboBox" Width="80">
                <ComboBoxItem Content="1"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="3"/>
                <ComboBoxItem Content="4"/>
                <ComboBoxItem Content="5"/>
                <ComboBoxItem Content="6"/>
                <ComboBoxItem Content="7"/>
                <ComboBoxItem Content="8"/>
                <ComboBoxItem Content="9"/>
                <ComboBoxItem Content="10"/>
                <ComboBoxItem Content="11"/>
                <ComboBoxItem Content="12"/>
            </ComboBox>

            <TextBlock Text="Năm:" FontSize="16" Margin="20,0,10,0" VerticalAlignment="Center"/>
            <TextBox Name="YearTextBox" Width="80"/>

            <Button Content="Xác nhận" Width="100" Height="30" Margin="20,0,0,0"
                    Background="#007BFF" Foreground="White" FontWeight="Bold" Click="GenerateReport_Click"/>
            <Button Content="Quay lại" Width="100" Height="30" Margin="20,0,0,0"
Background="#DC3545" Foreground="White" FontWeight="Bold"
Click="Back_Click"/>
        </StackPanel>

        <!-- Bảng hiển thị báo cáo (ListView) -->
        <ListView Name="ReportListView" Grid.Row="2" BorderThickness="1" BorderBrush="Gray"
                  Background="White" AlternationCount="2" Margin="0,10,0,0">
            

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Họ và tên" Width="150" DisplayMemberBinding="{Binding FullName}"/>
                    <GridViewColumn Header="Tổng ngày đi làm" Width="150" DisplayMemberBinding="{Binding TotalWorkDays}"/>
                    <GridViewColumn Header="Tổng ngày nghỉ có phép" Width="150" DisplayMemberBinding="{Binding TotalLeaveDays}"/>
                    <GridViewColumn Header="Tổng ngày nghỉ không phép" Width="170" DisplayMemberBinding="{Binding TotalUnexcusedLeaveDays}"/>
                    <GridViewColumn Header="Tổng giờ làm trong tháng" Width="150" DisplayMemberBinding="{Binding TotalWorkHours}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>