<Window x:Class="WPF.Admin.EmpDepList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Admin"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="EmpDepList" Height="450" Width="1000">
    <DockPanel LastChildFill="True">
        <!-- Sidebar (Nút Quay Lại) -->
        <StackPanel Width="200" Background="#E65100" Orientation="Vertical" DockPanel.Dock="Left">
            <Button Content="🔙 Quay Lại" Margin="10" Height="50" Background="LightGray" Foreground="White" Cursor="Hand" Click="Button_Click"/>
        </StackPanel>
        <Grid Background="#FFE0B2">
            <TextBlock Text="Danh Sách Nhân Viên" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="10"/>

            <ListView Name="EmployeeListView" Margin="10,50,10,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding EmployeeId}" Width="50"/>
                        <GridViewColumn Header="Tên Nhân Viên" DisplayMemberBinding="{Binding FullName}" Width="150"/>
                        <GridViewColumn Header="Số Điện Thoại" DisplayMemberBinding="{Binding PhoneNumber}" Width="150"/>
                        <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="180"/>
                        <GridViewColumn Header="Chức Vụ" DisplayMemberBinding="{Binding Position.PositionName}" Width="150"/>
                        <GridViewColumn Header="Xóa" Width="70">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗑 Xóa" Width="50" Margin="5" Background="#D32F2F" Foreground="White" Tag="{Binding EmployeeId}" Click="DeleteButton_Click"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
