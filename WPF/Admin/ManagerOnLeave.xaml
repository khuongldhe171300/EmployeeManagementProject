<Window x:Class="WPF.Admin.ManagerOnLeave"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.Admin"
        xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Quản lý nghỉ phép"
        Height="500" Width="900"
        Background="#F4F6F9">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" VerticalAlignment="Center">
            <TextBlock Text="🔍 Tìm kiếm: " FontWeight="Bold" VerticalAlignment="Center" Foreground="#555"/>
            <TextBox Width="250" x:Name="txtSearch"
                     material:HintAssist.Hint="Nhập từ khóa..."/>
            <Button Content="Tìm" Width="90" Margin="10 0 0 0" Click="Search_Click"
                    Background="#4CAF50" Foreground="White"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="5"
                BorderBrush="#ddd" BorderThickness="1">
            <ListView x:Name="LeaveRequestListView" SelectionChanged="LeaveRequestListView_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding LeaveRequestId}" Width="50"/>
                        <GridViewColumn Header="Mã NV" DisplayMemberBinding="{Binding Employee.FullName}" Width="70"/>
                        <GridViewColumn Header="Loại nghỉ" DisplayMemberBinding="{Binding LeaveType}" Width="120"/>
                        <GridViewColumn Header="Bắt đầu" DisplayMemberBinding="{Binding StartDate}" Width="100"/>
                        <GridViewColumn Header="Kết thúc" DisplayMemberBinding="{Binding EndDate}" Width="100"/>
                        <GridViewColumn Header="Số ngày" DisplayMemberBinding="{Binding TotalDays}" Width="80"/>
                        <GridViewColumn Header="Lý do" DisplayMemberBinding="{Binding Reason}" Width="200"/>
                        <GridViewColumn Header="Trạng thái" DisplayMemberBinding="{Binding Status}" Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Width="140" Margin="5" Click="Add_Click"
                    Background="#2196F3" Foreground="White">
                <StackPanel Orientation="Horizontal">
                    <material:PackIcon Kind="Plus" Width="20" Height="20" Margin="0 0 5 0"/>
                    <TextBlock Text="Thêm mới"/>
                </StackPanel>
            </Button>

            

            <Button Width="120" Margin="5" Click="Delete_Click"
                    Background="#F44336" Foreground="White">
                <StackPanel Orientation="Horizontal">
                    <material:PackIcon Kind="Delete" Width="20" Height="20" Margin="0 0 5 0"/>
                    <TextBlock Text="Từ chối"/>
                </StackPanel>
            </Button>

            <Button Width="140" Margin="5" Click="Approve_Click"
                    Background="#4CAF50" Foreground="White">
                <StackPanel Orientation="Horizontal">
                    <material:PackIcon Kind="CheckBold" Width="20" Height="20" Margin="0 0 5 0"/>
                    <TextBlock Text="Phê duyệt"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
